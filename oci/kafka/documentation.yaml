version: 2
application: kafka
description: |
  Apache Kafka is an open-source distributed event streaming platform for
  high-performance data pipelines, streaming analytics and integrations.
  
  Please note that the images tagged up to 3.1 are Dockerfile-base images,
  whereas from version 3.6.1 onward the images are now rocks.
website: https://kafka.apache.org
issues: https://github.com/canonical/kafka-rock/issues
source-code: https://github.com/canonical/kafka-rock

docker:
  parameters: >-
    -p 9092:9092
  run_conclusion: |
    Kafka starts and exposes broker port 9092.

config:
  TZ:
    type: env
    description: Configure the container timezone value.
  ZOOKEEPER_HOST:
    type: env
    description: Configure the ZooKeeper hostname for Apache Kafka 3.
    default: 'zookeeper'
  ZOOKEEPER_PORT:
    type: env
    description: Configure the ZooKeeper port for Apache Kafka 3.
    default: '2181'
  '`-v <path>:/etc/kafka/server.properties`':
    type: mount
    description: Configure with a mounted Kafka server configuration file.
  '`-v <volume>:/var/lib/kafka`':
    type: mount
    description: Configure with a mounted volume for persistent broker data.
  '`-p <port>:9092`':
    type: port
    description: Expose the Apache Kafka broker port 9092 on the host.
  '`--detach`':
    type: other
    description: Run Kafka in background mode.
  '`--name <container-name>`':
    type: other
    description: Run Kafka with a custom container name.
