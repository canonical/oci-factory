version: 2

application: dotnet-runtime
description: |
  .NET is a free, cross-platform, open source developer platform for building many different types of applications. With .NET, you can use multiple languages, editors, and libraries to build for web, mobile, desktop, games, IoT, and more.
  Read [the .NET documentation](https://docs.microsoft.com/en-us/dotnet/core/deploying/) to learn how to deploy your .NET application with container images.

  ### About Chiselled Ubuntu

  **This image does not include bash nor a package manager nor the .NET SDK.**
  Read more about Chiselled Ubuntu for .NET, a new class of OCI images, on [the Ubuntu blog](https://ubuntu.com/blog/install-dotnet-on-ubuntu); reading how Canonical and Microsoft partner together to deliver and support .NET on Ubuntu.

  If you're looking to publish a self-contained .NET app, please have a look at the `ubuntu/dotnet-deps` repository.
  If you're looking to publish an ASP.NET app, please then look at the `ubuntu/dotnet-aspnet` repository.

docker:
  parameters: -e TZ=UTC
  run_cmd: exec dotnet /app/HelloWorld.dll
  run_conclusion: |
    The container will run the .NET application specified by the path.
    The container expects a published .NET application to be mounted or copied into the image.

    ### Run a .NET application

    Let's use the following HelloWorld application as an example:

    ```bash
    $ # Create a HelloWorld.csproj file with the following content
    $ cat <<EOF > HelloWorld.csproj
    <Project Sdk="Microsoft.NET.Sdk">

      <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
      </PropertyGroup>

    </Project>
    EOF

    # Create a Program.cs file with the following code
    $ cat <<EOF > Program.cs
    Console.WriteLine("Hello, World!");
    EOF

    # Publish the .NET application (you need the "dotnet10" package)
    $ dotnet publish -c Release -o app
    MSBuild version 17.8.27+3ab07f0cf for .NET
      Determining projects to restore...
      Restored /tmp/demo/HelloWorld.csproj (in 59 ms).
      HelloWorld -> /tmp/demo/bin/Release/net10.0/HelloWorld.dll
      HelloWorld -> /tmp/demo/bin/Release/net10.0/publish/

    # Now you can run the app with "ubuntu/dotnet-runtime:10.0-26.04_edge"
    $ docker run --rm -v $PWD/app:/app ubuntu/dotnet-runtime:10.0-26.04_edge exec dotnet /app/HelloWorld.dll
    Hello, World!
    ```

  dockerfile: |
    Here is an example Dockerfile to build your own .NET application image on top of a dotnet-runtime image:

    #### For rocks (9.0-25.04 and later)

    ```docker
    FROM ubuntu:26.04 AS builder

    RUN apt-get update && apt-get install -y dotnet10 ca-certificates

    # add your application code (the HelloWorld example from above),
    # and change the TargetFramework to net10.0 in the HelloWorld.csproj
    WORKDIR /source
    COPY . .

    # publish your .NET app
    RUN dotnet publish -c Release -o /app

    FROM ubuntu/dotnet-runtime:10.0-26.04_edge

    WORKDIR /app
    COPY --from=builder /app ./

    CMD ["exec", "dotnet", "/app/HelloWorld.dll"]
    ```

    #### For Legacy images (6.0, 8.0, 9.0-24.10)
    ```Dockerfile
    FROM ubuntu:24.04 AS builder

    # install the .NET 8 SDK from the Ubuntu archive
    # (no need to clean the apt cache as this is an unpublished stage)
    RUN apt-get update && apt-get install -y dotnet8 ca-certificates

    # add your application code (the HelloWorld example from above)
    WORKDIR /source
    COPY . .

    # publish your .NET app
    RUN dotnet publish -c Release -o /app

    FROM ubuntu/dotnet-runtime:8.0-24.04_beta

    WORKDIR /app
    COPY --from=builder /app ./

    ENTRYPOINT ["dotnet", "/app/HelloWorld.dll"]
    ```

  legacy:
    parameters: -e TZ=UTC
    run_cmd: /app/HelloWorld.dll
    run_conclusion: |
      The container will run the .NET application specified by the path.

      **Note:** Images tagged 6.0, 8.0 and 9.0-24.10 are Dockerfile-based images with `dotnet` as the entrypoint.
      From version 9.0-25.04 onward, images are rocks with `pebble enter` as the entrypoint.
      Read more on the [Rockcraft docs](https://canonical-rockcraft.readthedocs-hosted.com/en/latest/explanation/rocks/).

      ### Run a .NET application (Legacy images: 6.0, 8.0, 9.0-24.10)

      Let's use the following HelloWorld application as an example:

      ```bash
      $ # Create a HelloWorld.csproj file with the following content
      $ cat <<EOF > HelloWorld.csproj
      <Project Sdk="Microsoft.NET.Sdk">

        <PropertyGroup>
          <OutputType>Exe</OutputType>
          <TargetFramework>net8.0</TargetFramework>
          <ImplicitUsings>enable</ImplicitUsings>
          <Nullable>enable</Nullable>
        </PropertyGroup>

      </Project>
      EOF

      # Create a Program.cs file with the following code
      $ cat <<EOF > Program.cs
      Console.WriteLine("Hello, World!");
      EOF
      # Publish the .NET application (you need the "dotnet8" package)
      $ dotnet publish -c Release -o app
      MSBuild version 17.8.27+3ab07f0cf for .NET
        Determining projects to restore...
        Restored /tmp/demo/HelloWorld.csproj (in 59 ms).
        HelloWorld -> /tmp/demo/bin/Release/net8.0/HelloWorld.dll
        HelloWorld -> /tmp/demo/bin/Release/net8.0/publish/

      # Now you can run the app with "ubuntu/dotnet-runtime:8.0-24.04_stable"
      $ docker run --rm -v $PWD/app:/app ubuntu/dotnet-runtime:8.0-24.04_stable /app/HelloWorld.dll
      Hello, World!
      ```

website: https://dotnet.microsoft.com/
issues: https://github.com/canonical/dotnet-runtime-rock/issues
source-code: https://github.com/canonical/dotnet-runtime-rock

config:
  TZ:
    description: Set the timezone for the container
    type: env
    default: UTC
  DOTNET_SYSTEM_GLOBALIZATION_INVARIANT:
    description: Whether to use invariant globalization mode.
    type: env
    default: 'true'

override_tracks:
  # Tracks release from legacy repo
  6.0-22.04:
    end-of-life: '2024-11-12T00:00:00Z'
  6.0-22.10:
    end-of-life: '2023-07-20T00:00:00Z'
  7.0-22.10:
    end-of-life: '2023-07-20T00:00:00Z'
  7.0-23.04:
    end-of-life: '2024-05-14T00:00:00Z'
  8.0-24.04:
    end-of-life: '2026-11-10T00:00:00Z'
  9.0-24.10:
    end-of-life: '2025-07-10T00:00:00Z'
  # Tracks with different tag than build-base
  9.0-25.04:
    end-of-life: '2026-01-15T00:00:00Z'
