version: 2

application: ruby
description: |
  Ruby is a general-purpose programming language. It is interpreted, high-level,
  and dynamically typed. It supports multiple programming paradigms, including
  procedural, object-oriented, and functional programming.
website: https://www.ruby-lang.org/
issues: https://github.com/canonical/ruby-rock/issues
source-code: https://github.com/canonical/ruby-rock

docker:
  parameters: -d
  run_conclusion: |
    The container starts in detached mode.

config:
  RUBYOPT:
    type: env
    description: Ruby options applied at startup.
    default: "--disable-did_you_mean --disable-error_highlight --disable-syntax_suggest"
  GEM_PATH:
    type: env
    description: Colon-separated list of paths used to locate installed gems.
    default: "/usr/lib/ruby/gems/3.2.0/gems:/usr/share/rubygems-integration/all"

override_tracks:
  # Tracks with different tag than build-base
  3.3-25.04:
    end-of-life: '2026-01-15T00:00:00Z'
