# === Ubuntu base CVEs (no upstream fix available in 24.04 images) ===
# openssl/libssl3 - DHE key validation inefficiency; not exploitable inside Airflow runtime
CVE-2024-41996
# linux-pam - incomplete fix for CVE-2025-6020; privilege escalation requires local shell access
CVE-2025-8941
# shadow-utils - subuid range conflict; not relevant to immutable container users
CVE-2024-56433
# tar - path traversal requires user-supplied tar extraction; Airflow does not extract tar files
CVE-2025-45582
# coreutils chroot --userspec escape; no chroot used inside Airflow runtime
CVE-2016-2781
# gpgv compressed packet DoS; gpgv is never invoked by Airflow
CVE-2022-3219



# === Python ecosystem (unused or unexploitable in Airflow runtime) ===
# ray - arbitrary code execution via job submission API; ray is bundled but unused by Airflow
CVE-2023-48022

# === Python tooling (not used at runtime) ===
# pip - symlink validation during tar extraction; pip not invoked inside running container
CVE-2025-8869
# werkzeug DoS via crafted multipart form; not used by Airflow webserver
CVE-2023-46136
# flask-appbuilder password reset bypass; Airflow bundles patched UI routes
CVE-2025-58065
